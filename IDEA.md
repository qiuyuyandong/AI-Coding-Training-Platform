# AI 代码成长与学习导航平台：产品说明

最后更新：2026-07-14
当前状态：产品方向已收敛，项目仍处于 **Pre-V0 技术原型**

> 本文是产品定义的唯一主入口：说明要解决什么问题、服务谁、核心机制和版本边界。
> 当前已经实现什么以 `README.md` 和 `docs/architecture.md` 为准；开发顺序与验收门槛以 `docs/superpowers/plans/2026-07-11-product-development-roadmap.md` 为准。

## 1. 一句话定位

这是一个面向计算机专业初学者的 **学习导航、代码训练与能力成长系统**。

它不重新制作完整网课，也不只是统计在多个 OJ 刷了多少题。它把职业方向、共同基础、知识图谱、优质资源、代码题、本地实现、阶段项目和训练证据连接起来，持续回答五个问题：

1. 不同计算机职业分别在做什么，需要哪些能力；
2. 我目前处于知识体系的什么位置；
3. 我今天最值得完成的一个任务是什么；
4. 我在各平台和本地完成的训练说明了什么；
5. 下一步应该学习、复习、练习还是做项目。

产品的核心不是“替用户学习”，而是把浩瀚、分散的学习选择变成一条能开始、能验证、能调整的成长路径。

## 2. 首批用户与暂不服务的人群

### 2.1 首批用户

第一批只服务作者本人和舍友，规模约 2～10 人，重点是大一计算机相关专业学生。用户主要处于以下两种水平：

- 学过 C++ 等语言的基础语法，但简单 OJ 题仍不能稳定独立完成；
- 能完成部分简单题，但知识零散，不会把课程知识组合成完整项目。

共同环境：

- 以 Windows 笔记本为主；
- 主要处于国内网络环境；
- 已经在力扣、洛谷、牛客等网站之一做过题；
- 想提升代码能力，但经常因为“不知道学什么、做什么”而无法开始。

### 2.2 V1 暂不重点服务

- 完全没有接触过编程的零基础用户；
- 以算法竞赛高强度训练为主要目标的用户；
- 只想进行短期面试冲刺的用户；
- 已具备成熟工程经验、只需要高级专项训练的开发者。

先把一个窄人群的闭环做真，再扩大范围。

## 3. 核心问题与产品假设

### 3.1 三层问题

1. **方向问题**：不知道目标职业需要学习哪些课程、书籍、知识点和工程能力。
2. **行动问题**：即使知道一些内容，也不知道从哪里开始、今天先做什么，选择成本造成启动阻力。
3. **能力问题**：学校课程和孤立刷题不能自然形成调试、测试、设计、整合与项目能力。

### 3.2 北极星闭环

```text
看见职业方向与当前路线位置
  → 获得一个有依据的默认任务
  → 一次点击进入资源、原 OJ 或本地工作区
  → 自动或低摩擦记录过程
  → 形成可解释的学习证据
  → 更新能力状态、复习队列和下一步计划
```

### 3.3 必须尽早验证的假设

| 假设 | 观察信号 | 失败时优先检查 |
|---|---|---|
| 一个默认任务能降低启动阻力 | 打开后能在几分钟内开始 | 任务太难、理由不可信、入口太多 |
| 推荐理由能建立信任 | 用户能复述“为什么是它” | 证据不足、解释太长、路线不可信 |
| 自动采集加少量补充足以形成可信证据 | 记录与用户回忆一致 | 适配器误报、缺少 session、人工补充太烦 |
| 图谱帮助定位而非增加负担 | 用户能找到现在与下一步 | 节点太多、关系太杂、缺少行动入口 |
| 计划允许调整后仍能保持方向 | 调整后仍会完成 | 自由度过大或系统约束过强 |
| 小规模内容维护成本可承受 | 每个节点能持续复核 | 节点粒度失控、资源太多、版本无治理 |

## 4. 当前项目真实状态

当前仓库不是完整 V1，而是一个可演示的本地训练采集原型。

### 4.1 已可复用

- Next.js App Router、TypeScript、SQLite 与基础页面；
- Chrome MV3 扩展的页面检测、提交点击和可见 verdict 检测；
- `capture_events` 原始事件与 `training_attempts` 物化记录；
- 隔离的 Playwright 数据库、V2 session/submission、SPA 生命周期与串行队列；
- localhost 配对凭证、内容敏感的 replay/idempotency 处理；
- canonical URL、题目范围查询与全量 Growth 聚合；
- 自动/手动来源、手工记录、有限字段纠错历史与逻辑作废；
- `/training`、规则式 `/coach`、基础 `/growth`；
- 题目元数据保存与原站跳转边界；
- Vitest、Playwright 和扩展构建基础。

### 4.2 仍只是原型

- 五个平台只是宽泛页面启发式，不等于稳定适配；
- 目前不能采集代码快照、运行测试、提示使用或可靠提交序列；
- Coach/Growth 仍只基于 attempt 结果做确定性统计，不能诊断知识薄弱点；
- 尚无通过真实页面夹具认证的 production-ready OJ 适配器；
- 还没有代码快照、运行测试证据或完整的学习能力模型。

### 4.3 尚未实现

- 职业地图、知识图谱、资源目录和版本化课程包；
- 用户目标、短诊断、今日计划和完整计划；
- 五级能力模型、复习调度和证据解释；
- VS Code 扩展与本地训练工作区；
- 真实 AI provider、默认 AI 额度、BYOK 与云端账户；
- 可用于公开产品的数据隔离、删除、导出和计费能力。

所以当前仍处于 **Pre-V0 可信原型**：0A—0C2 已完成，剩余生产适配器验证与 0D 工程质量门后，才能关闭 Phase 0。

## 5. 产品结构：共同基础 + 职业方向

### 5.1 共同基础

第一条可执行路线为 `Software Development Foundations v1`，逐步覆盖：

1. C++ 与基本编程；
2. 调试、命令行、Git、构建和测试；
3. 数据结构与算法；
4. 计算机组成、操作系统、网络和数据库的基础方向；
5. 软件工程与项目实践。

第一版不把所有内容都做深。V0 只发布 12～18 个可观察、可训练的能力节点，并用粗粒度骨架展示未来路线。

### 5.2 九个职业方向骨架

第一版提供方向认识与差异说明，不同时建设九套完整课程：

1. 前端与 Web 应用；
2. 后端与服务端；
3. 客户端开发；
4. 数据分析与数据工程；
5. 人工智能与机器学习；
6. 边缘 AI 与智能嵌入式；
7. 云计算、运维与平台工程；
8. 网络与信息安全；
9. 系统与底层软件。

每个方向先回答：主要解决什么问题、典型岗位、依赖哪些共同基础、额外能力模块、大致学习顺序和代表项目。

用户可以不选方向；系统建议选择 **1 个主方向 + 1～2 个备选方向**。未选择时先学习共同基础，后续根据兴趣与训练证据再调整。

## 6. 知识图谱与资源体系

### 6.1 分层图谱

```text
职业方向 / 能力领域
  → 课程或知识模块
  → 章节与主题
  → 可训练的具体能力
```

“具体能力”是记录个人状态的最小单位，例如“能识别并独立应用有序数组双指针”，而不是笼统的“学会数据结构”。

显示规则：

- 远景只看方向、课程和整体进度；
- 放大后逐步展开章节和具体能力；
- 默认突出当前节点、关键前置和近期后续；
- 主图默认只显示“必须前置”和“建议前置”；
- 相似、替代、应用于、项目验证等关系按需筛选；
- 图形不是唯一入口，必须同时提供可搜索、可键盘操作的列表视图。

### 6.2 公共图谱与个人路线分离

- 平台维护统一、版本化的公共知识关系；
- 用户可以隐藏、跳过、调整个人优先级，但不能修改公共事实；
- AI 可以建议个人路线变化，不能直接修改公共图谱；
- 跳过必须前置时先给短诊断；通过后记为“已验证跳过”，坚持跳过则记为“风险跳过”；
- 后续出现关联困难时，系统重新建议补学前置。

同一能力使用统一等级，但不同职业方向可以规定不同目标等级。用户达到当前方向要求后仍可继续进阶。

### 6.3 资源和题目如何进入图谱

- 书籍、文章、视频、课程和 OJ 题目挂载在能力节点详情，不默认塞进主图；
- 项目作为连接多个能力节点的综合验证节点；
- 每个正式资源保存来源、推荐理由、适合基础、学习范围、停止位置、语言、价格、国内可达性、许可边界、审核状态和复核日期；
- AI 可以发现候选资源，只有人工审核后才能进入正式推荐；
- 课程包与个人学习状态分离，并使用稳定 ID、语义版本和迁移映射。

## 7. 首次使用与每日学习闭环

### 7.1 首次使用

```text
浏览职业地图
  → 选择主方向/备选方向，或跳过
  → 完成短问卷和少量诊断任务
  → 获得建议起点
  → 用户可在图谱上手动调整
  → 后续训练持续修正判断
```

系统不通过一次测试给用户贴“适合/不适合某职业”的标签。

### 7.2 每日总控台

用户每天先选择可用时间：`15 / 30 / 60 / 90 分钟`。这是投入边界，不是预计完成时间，不用于和用户实际耗时比较。

首页同时提供：

- 一个最重要的核心任务；
- 大多数学习日的一道关联题；
- 满足时间和前置条件时出现的可选项目任务；
- 当前位置、当前阶段和完整计划入口；
- 局部知识图谱；
- 最多三个有明确角色的备选任务；
- 与 AI 讨论计划的入口。

后台每天从五种模式中选择主要模式：

- **学习**：接触一个小的新知识；
- **复习**：处理到期或低置信度能力；
- **练习**：完成针对性代码题；
- **构建**：推进项目里程碑；
- **恢复**：中断或连续受挫后，用更小任务重新开始。

### 7.3 有边界的自主调整

- 用户可一键换成轻量热身、同目标换题或旧弱点复习；
- 调整原因可选，不强制写长说明；
- 多天积压、方向切换或阶段重排由 AI 协助生成变更方案；
- 所有方案经过前置知识、复习、时间预算、认知负担和恢复规则检查；
- 用户确认差异后才生效；
- 漏掉一天不会触发惩罚，也不会把全部积压任务塞到第二天。

## 8. 代码训练、数据采集与本地工作区

### 8.1 外部 OJ 边界

V1 对商业 OJ 只保存：平台、题目 ID、标题、公开难度/标签、原题链接、知识映射和用户训练记录。用户点击后前往原站；不批量复制完整题面、隐藏测试、题解或会员内容。

需要增加 `canonical_problem` 层，避免同一道经典题在不同平台被误算成多次独立迁移证据。

### 8.2 浏览器扩展

围绕明确事件采集，而不是记录每次按键：

```text
进入题目
  → 点击运行/提交时保存代码快照
  → 记录可见评测结果和错误
  → 关联同一 submission/session
  → 再次提交时保存新快照并计算差异
```

可采集字段以平台实际公开给用户的内容为限：题目信息、语言、时间、代码快照、verdict、可见错误、公开运行时间/内存/得分、题解或提示访问状态。

不采集 Cookie、Token、密码、隐藏测试、完整浏览历史和每次键盘输入。每个平台使用独立适配器并声明证据覆盖等级；自动采集失败时必须有手动记录后备入口。

### 8.3 VS Code 训练工作区

VS Code 工作区与直接在 OJ 网页编辑器做题 **并列存在**：

- 直接在 OJ 做题是最低阻力兼容路径；
- VS Code 工作区是培养本地运行、测试、调试、文件组织与工程习惯的增强路径。

训练工作区按题目保存 `solution`、测试、笔记和题目元数据。VS Code 扩展记录指定工作区内的保存、运行、测试和调试事件；浏览器扩展记录在线提交；后端把两端事件合并为训练 session。

Git 只保存开始、通过、优化、复盘等有意义的里程碑，不承担连续训练日志，也不对每次保存自动 commit。

V1 不使用 MCP 传输高频训练事件；网页、浏览器扩展和 VS Code 扩展通过普通 HTTPS API 与事件 ID 协作。MCP 只作为未来向第三方 AI 客户端开放学习工具的可选能力。

## 9. 训练证据与能力判断

### 9.1 四层模型

```text
原始事实事件
  → 单次训练结论
  → 节点能力投影
  → 用户可见等级与解释
```

这样避免把 verdict、训练结果和“掌握”混为一谈。

### 9.2 单次训练的五种结论

1. **独立有效完成**：有较强独立解决证据；
2. **辅助后有效完成**：使用提示或题解，但形成了可继续验证的理解；
3. **有效挣扎**：尚未 AC，但错误、测试或代码明显向正确方向推进；
4. **无效试错**：重复提交却没有有意义的变化；
5. **证据不足**：只有最终结果或数据异常，暂不下强结论。

没有 AC 也可能发生学习；一次 AC 也不能证明稳定掌握。

### 9.3 具体能力的五级制

“未评估”独立于等级：

| 等级 | 可观察含义 |
|---|---|
| L1 初步理解 | 能识别概念或题型，在帮助下解释基本原理 |
| L2 辅助应用 | 借助示例、模板、提示或 AI 完成熟悉任务 |
| L3 独立应用 | 能在目标难度下独立完成新的对应任务 |
| L4 稳定掌握 | 跨时间、跨变式反复成功，并能有效调试 |
| L5 迁移综合 | 能用于更难问题、项目或相邻场景，并解释取舍 |

内部还保存连续掌握估计、置信度、稳定性、证据数量、最近验证时间、目标职业要求和等级原因，但界面不展示没有校准依据的小数精度。

### 9.4 保守更新规则

- 时间流逝只降低置信度或标记“需要刷新”，不自动降低可见等级；
- 只有新的表现证据才能降级；单次失败默认不降级；
- 一道题的主能力获得主要证据，次能力只获得较弱支持证据；
- 同题短期重复收益递减；延迟无提示重做、不同解法和复杂度优化仍可提供强证据；
- 用户可以自评和提出异议，但不能直接把 L2 改成 L4；系统通过新诊断重新评估；
- 每次等级变化显示关键证据、置信度、还缺什么和下一步，并可展开完整时间线与代码差异。

首版使用可解释规则，不训练知识追踪模型。证据权重、复习间隔和阈值全部是待试用校准的实验参数。

## 10. AI 的职责、模型与额度

### 10.1 AI 在系统中的位置

```text
训练事件与代码差异
  → 结构化证据提取
  → 规则引擎 + 知识图谱生成候选结论
  → AI 解释、复盘、协商计划和生成建议
```

AI 可以：解释当前路线位置、总结证据、分析可能错因、生成短复盘问题、在合法候选中调整任务、讨论目标变化和引用已审核资源。

AI 不能：凭空发布课程、因一次 AC 宣布掌握、绕过前置规则、直接改能力等级、引用不存在的证据或自动发布未审核资源。

### 10.2 三层模型服务

1. **平台默认模型**：为免费用户提供有限分析额度，保证首次体验；
2. **用户自带 API Key**：使用自己的额度与供应商，仍复用平台的图谱、证据和输出结构；
3. **未来领域模型**：积累真实且经过后续验证的数据后，再考虑小模型或微调，不从头训练基础大模型。

额度用完后，用户可以等待恢复、使用自己的 Key，或在公开商业版中升级付费方案。

后端使用 provider-neutral adapter，不把产品锁死在单个中转商。核心通道成熟后优先直连一至两个供应商，路由服务只承担探索和备用。

所有 AI 输出必须通过 schema 校验，引用真实 evidence ID，并有固定回归案例检查“一次 AC 误判掌握”“推荐未审核资源”“越权修改计划”等问题。

## 11. 数据架构、隐私与公开化路线

### 11.1 两个明确阶段

**当前本地试验阶段**：本地 Next.js + SQLite，数据默认留在设备，用于本人和舍友快速验证核心闭环。

**目标公开产品阶段**：云端 SaaS 负责账号、公共图谱、个人画像、计划、分析和跨设备同步；浏览器/VS Code 扩展是采集端并保留离线缓存。公开版使用多租户数据库和对象存储，不能把 Vercel 的临时文件系统或一份共享 SQLite 当生产存储。

云端是已确定的目标方向，但必须在核心闭环验证后通过独立架构与合规门槛上线，不能把“未来公开”伪装成“当前已经具备”。

该决策的技术边界记录在 [`docs/decisions/0001-local-pilot-to-cloud-saas.md`](docs/decisions/0001-local-pilot-to-cloud-saas.md)。

### 11.2 三档采集模式

| 模式 | 内容 | 分析能力 |
|---|---|---|
| 完整分析 | 指定训练工作区/提交事件的代码快照、运行结果、OJ 记录 | 最完整 |
| 基础分析 | 代码差异特征、错误类型、提交结果，不保留完整代码 | 中等 |
| 最小记录 | 题目、时间、verdict、完成状态 | 较低 |

首次配置可预选“完整分析”，但必须清楚展示范围并由用户确认；用户可随时关闭、查看、导出或删除历史快照。关闭只降低判断置信度，不影响继续使用。

永远不自动上传：API Key、环境变量、`.env`、SSH Key、Cookie/Token、工作区外文件、完整终端历史和未授权仓库。

### 11.3 必须分开的授权目的

以下目的不能合并成一句“帮助改进服务”：

1. 为用户完成本次分析；
2. 保存代码快照以形成长期个人报告；
3. 使用匿名化结构化指标改进规则与产品；
4. 使用原始代码或 AI 对话训练通用模型。

前三项按功能需要和清晰设置处理；第四项在公开版上线前必须单独完成法律、保留期限、删除和供应商评估，不能仅依靠模糊默认勾选。原始代码用于通用模型训练应采用独立明确选择。

## 12. 产品版本与范围上限

### Pre-V0：可信原型

- 修复 E2E 数据隔离、session/submission、SPA/pagehide、队列和真实聚合；
- 增加 canonical URL、来源凭证和手动记录 fallback；
- 只把一个 OJ 适配器做稳，其余明确标为实验；
- 当前候选首发平台为洛谷，Phase 0 用真实页面夹具验证后冻结。

### V0：手动优先的最小学习闭环

- 12～18 个已审核能力节点和 20～40 个资源/题目入口；
- 职业方向粗骨架、只读小图谱、目标与短诊断；
- `/today`、`/plan` 与手动训练记录；
- 15/30/60/90 分钟、一个主任务和少量备选；
- 最简五级状态与可解释规则；
- 规则式 Coach，AI 只做一个可关闭的薄切片；
- 自己使用 1 周，1～2 位舍友使用 2 周。

### V0.5：一个 OJ 的自动采集与基础证据

- 一个真实稳定的浏览器平台适配器；
- 提交序列、代码快照、verdict、语言和可见错误；
- 代码 diff、五类训练结论和节点状态更新；
- 完整/基础/最小采集模式；
- provider-neutral AI adapter、有限试用额度和 BYOK；
- 2～5 人连续使用 2～4 周。

### V1：完整学习导航与本地工程训练

- 分层交互知识图谱和九个职业方向浅层骨架；
- VS Code 扩展、训练工作区和 OJ session 合并；
- 复习、变式、迁移与一个阶段项目；
- AI 有边界地解释证据和提议计划变更；
- 数据查看、导出、删除和保留策略；
- 5～10 人四周有效学习闭环试用。

### Public Beta：公开 SaaS

- 账号、多租户隔离、云数据库与对象存储；
- 平台默认 AI 额度、滥用防护和成本控制；
- BYOK 的加密密钥存储；
- 同意、撤回、导出、删除、保留期限和第三方供应商披露；
- Chrome/Edge 分发与 Windows 安装体验；
- 免费版与付费版，支付在学习闭环和成本模型验证后再接入。

V1 之前不做：多 OJ 全覆盖、社区、排行榜、积分、自研基础大模型、MCP、在线 IDE、自建判题、日历导入和九条完整职业课程。

## 13. 四周验证标准

第一核心指标是 **四周有效学习闭环达成率**，不是注册量、连续打卡或总题量。

首轮实验暂定同时观察：

- 每周至少 3 次有效训练；
- 完成当周约 60%～70% 的核心任务；
- 至少完成 1 个阶段目标；
- 至少 1 个能力节点产生可验证提升；
- 用户能理解系统为什么更新能力状态和后续路线。

这些数字是首轮试验门槛，不是永久科学标准。辅助指标包括任务启动率、替换率、采集成功率、误归因率、延迟复测、迁移表现、推荐信任度、AI 使用率和恢复训练比例。

每个 Phase 都要尽早让本人或舍友试用；Phase 6 的四周试用是正式校准，不是第一次见用户。

## 14. 决策状态

### 已采用

- 共同基础做深、九个职业方向先做浅层骨架；
- 分层公共图谱，个人路线可调但不改公共关系；
- 每天一个核心任务，大多数学习日配一题；
- 15/30/60/90 分钟投入边界；
- 有边界的任务替换和 AI 计划调整；
- 五类训练结论与 `未评估 + L1～L5` 能力等级；
- OJ 元数据 + 原站跳转，不复制完整商业题面；
- 浏览器扩展与 VS Code 扩展分工，Git 只做里程碑；
- 规则/图谱权威，AI 负责解释和提议；
- 平台有限 AI + BYOK，公开版最终采用云端 SaaS。

### 实验参数

- 节点数量、任务数量、复习间隔、证据权重和置信度阈值；
- 每日时间档对应的任务容量；
- 洛谷作为首发平台候选；
- 四周、每周三次和 60%～70% 完成率。

### 后期

- 日历/课程表导入；
- 多平台全面同步；
- 完整职业分支；
- 内容管理后台；
- 自制大题库、在线 IDE 和判题；
- MCP 与领域模型。

### 公开版上线前必须单独确认

- 云数据库、对象存储和多租户方案；
- 代码快照保留期限与删除策略；
- 平台默认模型、供应商和免费额度；
- BYOK 密钥加密方案；
- 原始代码/AI 对话用于模型改进的独立授权；
- 定价、支付与未成年人相关合规边界。

## 15. 主要风险与控制

- **范围失控**：用 V0/V0.5/V1 纵向切片，不同时深化九条职业路线。
- **图谱变成装饰**：每个已发布能力节点必须连接学习成果、资源和可执行任务。
- **伪精确**：显示等级、证据和不确定性，不宣传未经校准的百分比。
- **无限刷题**：一次只追加一个最有诊断价值的任务，失败时可回到前置、资源或小实现。
- **错误采集污染画像**：Phase 0 先解决 session、idempotency、来源、SPA、队列和测试隔离。
- **资源失效与版权**：只保存元数据和合法入口，记录许可、复核日期和国内访问替代。
- **AI 越权与提示注入**：上下文 allowlist、证据引用、schema 校验、变更 diff 和人工确认。
- **云端隐私与成本**：分级采集、用途分离、保留期限、删除导出、供应商披露和额度限制。
- **安装阻力**：Chrome/Edge 双路径、Windows 启动诊断；是否桌面封装由真实试用决定。

## 16. 参考框架

- ACM/IEEE-CS/AAAI CS2023：https://csed.acm.org/
- IEEE SWEBOK：https://www.computer.org/education/bodies-of-knowledge/software-engineering
- OSSU Computer Science：https://github.com/ossu/computer-science
- Teach Yourself CS：https://teachyourselfcs.com/
- MIT OpenCourseWare：https://ocw.mit.edu/
- Nand2Tetris：https://www.nand2tetris.org/course
- MIT Missing Semester：https://missing.csail.mit.edu/
- GitHub Skills：https://skills.github.com/
- Bloom, Learning for Mastery：https://eric.ed.gov/?id=ED053419
- Retrieval Practice：https://pubmed.ncbi.nlm.nih.gov/16507066/
- FSRS：https://github.com/open-spaced-repetition/fsrs4anki

## 17. 当前下一步

下一步先完成 production-ready OJ 适配器夹具/认证与 Phase 0D 工程质量门，再在不等待完整图谱的前提下制作一个 V0 纵向切片：

```text
一个明确用户
  + 12～18 个能力节点
  + 一个目标与短诊断
  + 一个今日任务
  + 一个手动训练记录
  + 一次状态更新
  + 一周真实自用
```

详细顺序、版本映射和每阶段退出条件见 [`docs/superpowers/plans/2026-07-11-product-development-roadmap.md`](docs/superpowers/plans/2026-07-11-product-development-roadmap.md)。
