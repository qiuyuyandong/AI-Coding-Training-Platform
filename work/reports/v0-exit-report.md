# V0 Exit Report

> **Status correction (2026-07-18): SUPERSEDED / NO CURRENT EXIT DECISION.**
> The earlier report declared an exit candidate while the required owner and
> participant observation artifacts contained no sessions. That decision is
> invalid and must not be used as release authority.

The active recovery plan is
`docs/superpowers/plans/2026-07-18-v0-closeout-observation-final-verification.md`.
It replaces the self-referential five-SHA chain with two commits:

1. an immutable RC implementation commit (`implementationSha`); and
2. a final evidence/status commit after real observation, F1–F4 approval,
   explicit user acceptance, and `neat-freak` reconciliation.

This file intentionally contains no machine-readable accepted decision. A new
`ACCEPT_V0` report may be written only after both observation validators pass
against the unchanged RC SHA. Until then, V0 remains in validation and V0.5
implementation must not start.
